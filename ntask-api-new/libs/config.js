//  const config = () => {
//     const env = process.env.NODE_ENV;
//     if(env == "test"){
//         const config = require(`./config.${env}.js`);
//         return config;
//     }
//     const config = require(`./config.${env}.js`);
//     return config;
// };
// module.exports = config;

// //its not working